[@faasjs/node-utils](../README.md) / streamToString

# Function: streamToString()

> **streamToString**(`stream`): `Promise`\<`string`\>

Alias of `streamToText` for backward compatibility.

## Parameters

### stream

`ReadableStream`\<`Uint8Array`\>

## Returns

`Promise`\<`string`\>

## Throws

- Throws `TypeError` if stream is not a `ReadableStream` instance.
