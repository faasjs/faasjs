defaults:
  plugins:
    api:
      type: api
      config:
        env: defaults
    test:
      type: defaults
    http:
      type: http
      config:
        cookie:
          session:
            secret: test-secret
    mysql:
      type: mysql
local:
  plugins:
    api:
      local: local
    test:
      type: local
