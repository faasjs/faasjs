defaults:
  plugins:
    func:
      type: function
      config:
        env: defaults
    test:
      type: defaults
    http:
      type: http
    mysql:
      type: mysql
local:
  plugins:
    func:
      local: local
    test:
      type: local
