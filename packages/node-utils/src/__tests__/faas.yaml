defaults:
  plugins:
    func:
      type: function
      config:
        env: defaults
    test:
      type: defaults
    http:
      type: http
      config:
        cookie:
          session:
            secret: test-secret
    mysql:
      type: mysql
local:
  plugins:
    func:
      local: local
    test:
      type: local
