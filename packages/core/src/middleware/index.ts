/**
 * Middleware helpers re-exported from `@faasjs/core/middleware`.
 *
 * Use this entrypoint when you only need middleware adapters such as
 * `useMiddleware`, `useMiddlewares`, or `useStaticFile`.
 */
export * from './middleware'
export * from './static'
