[@faasjs/core](../README.md) / defineFunc

# Function: defineFunc()

Create a cloud function with optional params schema validation.

Plugins are auto-loaded from `func.config.plugins` and cannot be disabled.
