[@faasjs/react](../README.md) / InferRuleOption

# Type Alias: InferRuleOption\<T\>

> **InferRuleOption**\<`T`\> = `T` *extends* (`value`, `options`, `lang?`) => `Promise`\<`void`\> ? `O` : `never`

## Type Parameters

### T

`T`
