[@faasjs/dev](../README.md) / createPgliteKnex

# Function: createPgliteKnex()

> **createPgliteKnex**(`config?`, `connection?`): `Knex`

Create a knex instance backed by `knex-pglite`.

## Parameters

### config?

`Partial`\<`Knex.Config`\> = `{}`

### connection?

`Record`\<`string`, `unknown`\> = `{}`

## Returns

`Knex`
