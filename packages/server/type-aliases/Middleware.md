[@faasjs/server](../README.md) / Middleware

# Type Alias: Middleware()

> **Middleware**: (`request`, `response`, `logger`) => `void` \| `Promise`\<`void`\>

## Parameters

### request

`IncomingMessage`

### response

`ServerResponse`

### logger

`Logger`

## Returns

`void` \| `Promise`\<`void`\>
