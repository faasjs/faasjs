[@faasjs/workflow](../README.md) / WorkflowStepTarget

# Type Alias: WorkflowStepTarget

> **WorkflowStepTarget** = `object`

A target step to create after the current step.

## Properties

### name

> **name**: `string`

Step name from the workflow definition.

### params?

> `optional` **params?**: `unknown`

Params passed to the target step. Defaults to `{}`.
