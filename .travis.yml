language: node_js
node_js:
  - 12
cache: yarn
services:
  - redis-server
  - mysql
  - postgresql
  - mongodb
script:
  - cd examples/auth && yarn && yarn test
  - cd ../sql && yarn && yarn test
  - cd ../nuxt && yarn && yarn test
  - cd ../simple && yarn && yarn test
  - cd .. && yarn && yarn test
after_success:
  - yarn codecov
