{
  "name": "faasjs",
  "private": true,
  "scripts": {
    "test": "lerna bootstrap && lerna run ci",
    "pub:latest": "lerna exec -- npm dist-tag add \\$LERNA_PACKAGE_NAME@$(npm view . version) latest",
    "pub": "lerna publish --preid beta --pre-dist-tag beta"
  },
  "devDependencies": {
    "lerna": "*"
  }
}
