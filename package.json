{
  "name": "faasjs-mono",
  "version": "8.0.0-beta.9",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "pub": "npm run build && npm run build:doc && npm run lint && node publish.ts",
    "clean": "rm -rf packages/*/node_modules packages/*/dist packages/*/.turbo coverage packages/*/*.log tmp *.log .turbo",
    "build": "npm run lint && npm run clean && npm exec turbo build && rm -f packages/*/dist/*.d.cts",
    "build:doc": "node build-docs.ts",
    "lint": "oxlint --fix .",
    "ci": "vitest run --silent --coverage"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "*",
    "@testing-library/react": "*",
    "@testing-library/user-event": "*",
    "@types/node": "*",
    "@vitejs/plugin-react": "*",
    "@vitest/coverage-v8": "*",
    "jsdom": "*",
    "tsdown": "*",
    "turbo": "*",
    "typedoc": "*",
    "typedoc-plugin-markdown": "*",
    "typescript": "*",
    "vite-tsconfig-paths": "*"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@latest"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "packageManager": "npm@11.8.0"
}
